apiVersion: apps/v1
kind: Deployment
metadata:
  name: resale-queue-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resale-queue-app
  template:
    metadata:
      labels:
        app: resale-queue-app
    spec:
      containers:
        - name: resale-queue-app
          image: de.icr.io/dev-registry/loveresale-queue-management-backend:latest
          args:
            - --spring.application.json=$(BINDING)
          env:
            - name: BINDING
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: application-properties
            - name: DB_CONNECTION
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_CONNECTION
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_PASSWORD
          ports:
            - containerPort: 8025
            - containerPort: 9092 
          resources:
            requests:
              cpu: 125m
              memory: 350Mi
---
apiVersion: v1
kind: Service
metadata:
  name: resale-queue-service-cip
  labels:
    app: resale-queue-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8025

    - name: websocket
      port: 9092
      targetPort: 9092
  selector:
    app: resale-queue-app
